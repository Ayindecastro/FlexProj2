import { DefaultTheme } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#fff",
    secondary: "#fff",
    error: "#f13a59",
    success: "#00B386"
  }
};
